# x86 环境下构建多架构镜像（AMD64 + ARM64）

> 适用场景：在 x86 机器上构建同时支持 `linux/amd64` 和 `linux/arm64` 的 Docker 镜像，并推送到 Docker Hub。
>
> 全文分四块：**准备工作（首次）** → **构建推送（每次）** → **验证使用** → **问题排查**。按顺序执行即可。

---

## 一、准备工作（首次使用，做一次即可）

### 1、启用 BuildKit

修改 Docker 配置文件 `~/.docker/daemon.json`（Docker Desktop 在设置界面改也可），启用实验性功能并默认使用 BuildKit：

```json
{
  "experimental": true,
  "features": { "buildkit": true }
}
```

> 改完重启 Docker Desktop 生效。

### 2、安装 QEMU 模拟器

x86 机器要构建 ARM 镜像，需要 QEMU 模拟器注册所有架构：

```shell
# 拉取 binfmt 镜像
docker pull tonistiigi/binfmt:latest

# 注册所有架构的二进制模拟（ARM 等），x86 系统能因此构建 ARM 镜像
docker run --rm --privileged tonistiigi/binfmt:latest --install all
```

> 一次性操作，机器重启后通常仍有效。

### 3、创建 Buildx 构建器

```shell
# 创建名为 multi-builder 的构建器，driver 用 docker-container（支持多平台），目标平台 amd64+arm64
docker buildx create --name multi-builder --driver docker-container --platform linux/amd64,linux/arm64 --use
```

参数说明：
- `--driver docker-container`：替换默认驱动，支持真正的多平台构建。
- `--platform linux/amd64,linux/arm64`：指定目标架构。
- `--use`：创建后自动选为当前构建器。

**可选：配置镜像加速（pull 失败时加）**

如果后续拉取基础镜像经常失败，可建一个 `buildkitd.toml` 配置文件，内容：

```toml
[registry."docker.io"]
  mirrors = ["docker.m.daocloud.io", "docker.rainbond.cc", "docker.1ms.run"]
```

然后用该配置重建构建器：

```shell
docker buildx rm multi-builder
docker buildx create --name multi-builder --driver docker-container --platform linux/amd64,linux/arm64 --config buildkitd.toml --use
```

> ⚠️ **重要限制**：mirror **只代理镜像层数据的拉取**（把 `registry-1.docker.io` 的 pull 改走加速器），**不代理 push 时的 OAuth token 认证**（push 认证打的是 `auth.docker.io`，mirror 管不到）。所以 mirror 能救 pull，**救不了 push**。push 失败要走第 6 步的代理方案。

### 4、启动并验证构建器

```shell
# 引导启动构建器（首次会拉取 buildkit 镜像）
docker buildx inspect --bootstrap

# 检查构建器状态，确认 multi-builder 存在且 driver 为 docker-container
docker buildx ls

# 若状态异常，删除后重新开始（回到第 3 步）
docker buildx rm multi-builder
```

> 至此准备工作完成，后续每次构建直接从第二块开始。

---

## 二、构建与推送（每次发版都用）

### 5、构建多架构镜像并推送

进入 Dockerfile 所在目录，执行：

```shell
# --push 必须加：多架构镜像仅存于 Buildx 缓存，无法用 docker push 补推
docker buildx build --platform linux/amd64,linux/arm64 -t sawyerlsy/nginx:1.28.0 --push .
```

参数说明：
- `--platform linux/amd64,linux/arm64`：一次构建两个架构。
- `-t sawyerlsy/nginx:1.28.0`：目标镜像名和 tag（换成你自己的）。
- `--push`：构建完直接推到 Docker Hub。
- `.`：构建上下文为当前目录（Dockerfile 必须在此）。

**成功判断**：日志末尾出现 `pushing manifest for docker.io/...@sha256:...` 完成、且**无 ERROR**、回到命令提示符，即成功。

**失败判断**：出现 `ERROR: failed to build: failed to solve...` 和 `What's next: Debug this build failure`，去第 6 步或第 10 步排查。

### 6、代理配置（推送失败时的首选方案）

国内网络下，push 卡在 `auth.docker.io` 拿 OAuth token 是**最常见**的失败点（DNS 污染返回假 IP）。**首选方案：在 cmd 窗口临时设代理后直接 build，不用动 builder 配置。**

```cmd
:: 在 cmd 窗口设置代理（clash verge 默认 mixed-port 7897；clash 默认 7890，按实际填）
set HTTP_PROXY=http://127.0.0.1:7897
set HTTPS_PROXY=http://127.0.0.1:7897
set NO_PROXY=localhost,127.0.0.1

:: 同一窗口直接构建推送
docker buildx build --platform linux/amd64,linux/arm64 -t sawyerlsy/nginx:1.28.0 --push .
```

**原理**：`docker buildx` CLI 在 build 时会**把宿主机当前的 `HTTP_PROXY`/`HTTPS_PROXY`/`NO_PROXY` 环境变量通过 build request 传给 buildkit 容器**（BuildKit 的 proxy entitlement 机制）。所以 buildkit 容器虽独立，却能拿到宿主机代理设置，全流程（pull metadata、push auth、push layers）都走代理。

**注意事项**：
- `set` 只对**当前 cmd 窗口**有效，关掉就没了，每次新窗口要重新 set。
- 用 `127.0.0.1` 即可（宿主机本机回环），**不需要 clash 开 Allow LAN**。
- clash 必须开着；clash 关了 push auth 又会回到被污染状态。
- 想省事可写批处理 `build-push.bat`：第一行 set 代理，第二行 build 命令，以后双击调用。**不建议用 `setx` 写永久环境变量**，clash 没开时会影响其他程序联网。

---

## 三、验证与使用

### 7、查看远程镜像支持的架构

```shell
# 查远端 manifest（预期输出：显示 linux/amd64 和 linux/arm64 的 Manifest 条目）
docker buildx imagetools inspect sawyerlsy/nginx:1.28.0

# 或者用 manifest 命令（需要启用 experimental）
docker manifest inspect sawyerlsy/nginx:1.28.0
```

> ⚠️ imagetools inspect 不经过 builder，是宿主机直接发请求查远端。国内网络下同样会被 `auth.docker.io` 污染影响，要在**设了代理的 cmd 窗口**里跑（同第 6 步的 set）。
>
> 输出里除了 `linux/amd64` 和 `linux/arm64` 两个 Platform 条目外，还会有两个 `unknown/unknown` 的 attestation-manifest（SBOM/SLSA 出处证明），是 buildkit 默认生成的附加元数据，不是镜像本身，可忽略。

查看本地镜像是哪个架构：

```shell
docker inspect sawyerlsy/nginx:1.28.0 --format='{{.Architecture}}'
```

### 8、拉取指定架构的镜像

```shell
# 强制拉 arm64 版本（x86 机器拉了也只能模拟运行，见第 9 步）
docker pull --platform=linux/arm64 sawyerlsy/nginx:1.28.0
```

### 9、本地测试模拟运行

```shell
# 在 x86 机器上用 QEMU 模拟运行 arm64 镜像（验证镜像能否跑起来）
docker run --rm --platform linux/arm64 sawyerlsy/nginx:1.28.0
```

> 前提：第 2 步的 QEMU 模拟器已安装，否则无法在 x86 上跑 arm64 二进制。

---

## 四、问题排查

### 10、常见问题排查

构建日志要分阶段看，**卡在哪一步对应不同根因**：

**问题 A：卡在 `load metadata for docker.io/library/xxx` 或 `FROM ... resolve`**
- 阶段：pull 拉基础镜像元数据失败。
- 根因：`registry-1.docker.io` 直连被污染。
- 解法：加 `buildkitd.toml` mirror（第 3 步），或直接走代理（第 6 步，代理也能救 pull）。

**问题 B：卡在 `[auth] xxx:pull,push token for registry-1.docker.io`，报 `ERROR: failed to fetch oauth token: Post "https://auth.docker.io/token": dial tcp ...`**
- 阶段：push 拿 OAuth token 失败。
- 根因：`auth.docker.io` 被 DNS 污染（IPv6 假地址如 `2a03:2880:...`，或 IPv4 Dropbox 段如 `108.160.162.109`）。
- 这是**最常见**的失败点。mirror 救不了（mirror 不代理 `auth.docker.io`）。
- 解法：cmd `set` 代理后重跑（第 6 步）。重试 / 换国内网络一般无效，因为污染是普遍的。

**问题 C：构建器状态异常**
- 解法：`docker buildx rm multi-builder` 删除后回第 3 步重建。

**成功 vs 失败的日志对比**：
- ✅ 成功：末尾 `pushing manifest for docker.io/...@sha256:...` 完成，无 ERROR，回到提示符。
- ❌ 失败：显示 `ERROR: failed to build: failed to solve...` 和 `What's next: Debug this build failure`。

### 11、Docker Hub 三个端点说明（便于理解报错）

| 域名 | 作用 | 什么时候碰到 |
|------|------|-------------|
| `hub.docker.com` | 网页 UI | 浏览器看镜像、管理 tag |
| `registry-1.docker.io` | 镜像数据 API | push/pull 镜像层数据 |
| `auth.docker.io` | OAuth 认证 | 拿 push/pull 的 token（**最容易被污染**） |

日志里 `push token for registry-1.docker.io` 意思是"为了往 `registry-1.docker.io` 推，需要先去 `auth.docker.io` 拿 token"。push 目标是对的，死在拿 token 这一步。

---

## 附：日常操作速查

每次发版只需做两件事（前提：第 1-4 步已完成、clash 开着）：

```cmd
:: 1. 设代理（每次开新 cmd 窗口都要）
set HTTP_PROXY=http://127.0.0.1:7897
set HTTPS_PROXY=http://127.0.0.1:7897
set NO_PROXY=localhost,127.0.0.1

:: 2. 构建推送（换成你的镜像名/tag/路径）
docker buildx build --platform linux/amd64,linux/arm64 -t sawyerlsy/your-image:tag --push .
```
