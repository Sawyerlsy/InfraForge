通常采用nginx + keepalived的方式搭建高可用集群,需要配置nginx_check.sh

## x86环境下如何构建多架构镜像

### 1、启用BuildKit
```
# 启用 Docker 的实验性功能并默认使用 BuildKit 作为镜像构建引擎
{
"experimental": true,
"features": { "buildkit": true }
}
```

### 2、安装QEMU模拟器
```shell
# 拉取镜像
docker pull tonistiigi/binfmt:latest

# 安装 QEMU 模拟器,运行镜像并注册所有支持的架构.启用 ARM 等架构的二进制文件模拟，使 x86 系统能构建 ARM 镜像
docker run --rm --privileged tonistiigi/binfmt:latest --install all
```

### 3、创建 Buildx 构建器
```shell
# 创建 Buildx 构建器
docker buildx create --name multi-builder --driver docker-container --platform linux/amd64,linux/arm64 --use
```

>- --driver docker-container：替换默认驱动以支持多平台构建。
>
>- --platform：指定目标架构（AMD64 + ARM64）

### 4、启动Buildx并验证
```shell
docker buildx inspect --bootstrap
# 检查构建器状态
docker buildx ls
# 若状态异常，则删除后重新开始
docker buildx rm multi-builder
```

### 5、构建多架构镜像
```shell
# --push必须添加,因为镜像仅存于本地 Buildx 缓存，无法通过 docker push 补推
docker buildx build --platform linux/amd64,linux/arm64 -t sawyerlsy/nginx:1.28.0 --push .
```

### 6、拉取指定架构的镜像
```shell
docker pull --platform=linux/arm64 sawyerlsy/nginx:1.28.0
```

### 7、查看远程镜像支持哪些架构
```shell
# 检查镜像是否支持多架构(预期输出：显示 linux/amd64 和 linux/arm64 的 Manifest 条目)
docker buildx imagetools inspect sawyerlsy/nginx:1.28.0

# 或者使用manifest,需要启用experimental
docker manifest inspect sawyerlsy/nginx:1.28.0

# 查看本地镜像是哪个架构的
docker inspect sawyerlsy/nginx:1.28.0 --format='{{.Architecture}}'
```

### 8、本地测试模拟运行
```shell
docker run --rm --platform linux/arm64 sawyerlsy/nginx:1.28.0
```


