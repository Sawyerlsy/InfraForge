### 问题集锦
##### 1、银河麒麟系统可能预装了 Podman，而 Podman 与 Docker 的守护进程（dockerd）存在冲突。当两者同时运行时，可能导致权限问题
```shell
# 卸载 Podman
sudo yum remove podman -y
# 重启 Docker
sudo systemctl restart docker
```

##### 2、调整open files限制
CentOS系统
```shell
# 修改limits.conf,当前会话重新登录即可,已经运行的应用需要重启
cat >> /etc/security/limits.conf << EOF
* soft nofile 65535
* hard nofile 65535
* soft memlock unlimited
* hard memlock unlimited
* soft nproc 120000
* hard nproc 120000
EOF

# 查看是否生效
# open files：对应配置中的 nofile，应显示为 65535。
# max user processes：对应配置中的 nproc，应显示为 120000
ulimit -a
```

在Debian系的系统中，*对root用户无效，所以要单独指定root用户
```shell
# 修改limits.conf,当前会话重新登录即可,已经运行的应用需要重启
cat >> /etc/security/limits.conf << EOF
* soft nofile 65535
* hard nofile 65535
* soft memlock unlimited
* hard memlock unlimited
* soft nproc 120000
* hard nproc 120000
root soft nofile 65535
root hard nofile 65535
root soft memlock unlimited
root hard memlock unlimited
root soft nproc 120000
root hard nproc 120000
EOF

# 查看是否生效
# open files：对应配置中的 nofile，应显示为 65535。
# max user processes：对应配置中的 nproc，应显示为 120000
ulimit -a
```


```shell
# 如果没有生效,或者root生效而其他的用户不生效,则检测limits.d目录是否存在20-nproc.conf, 90-nproc.conf等文件
ls /etc/security/limits.d/

# 如果有对应的文件,则增加权限配置即可，如:
root       soft    nproc     unlimited
```
> 重新登录会话后配置才会生效,对于正在运行的应用,需要重启使配置生效
> - Docker应用：docker应用会受到容器启动参数或者docker daemon.json等的影响,重启docker后需要进入容器cat /proc/1/limits查看是否生效
> ```text
>   # 查看Docker服务本身的Max open files限制
>   - sudo systemctl show docker --property=LimitNOFILE
>   - sudo systemctl cat docker
> ```
> - 非Docker应用：重新登录后，重启应用即可
#### 3、内存敏感型的应用建议关闭swap
```shell
# 临时关闭swap
sudo swapoff -a

# 永久关闭swap 
sed -i '/swap/d' /etc/fstab # 删除包含swap的行

# 可选：调整内核参数（彻底禁用 Swap 倾向）
echo "vm.swappiness=0" | sudo tee -a /etc/sysctl.conf  # 禁止内核使用 Swap
sudo sysctl -p 

# 查看是否已禁用
free -hm
swapon --show
```

#### 4、内核参数调整
```shell
# 激进内存策略，避免申请失败，但需防范 OOM
echo "vm.overcommit_memory = 1" >> /etc/sysctl.conf
# 显著提升高并发服务的连接容量，需与应用配置协同
echo "net.core.somaxconn = 10240" >> /etc/sysctl.conf
sysctl -p

# 验证参数
cat /proc/sys/vm/overcommit_memory  # 应为 1
cat /proc/sys/net/core/somaxconn    # 应为 10240
```
> - Redis 集群部署时，这两个参数是必备优化项，可减少 Cannot allocate memory 错误和连接超时问题
> - 若物理内存紧张，优先考虑 vm.overcommit_memory=2 并增加 Swap 空间


#### 4、内存映射区域参数调整
> #### vm.max_map_count 控制一个进程可以拥有的内存映射区域(maximum memory map areas)的最大数量
> #### 适用场景：Elasticsearch、Redis、MongoDB等需要大量内存映射的数据库和应用
```shell
# 在文件末尾添加一行 vm.max_map_count=262144
echo "vm.max_map_count = 262144" | sudo tee -a /etc/sysctl.conf
# 重新加载sysctl配置，无需重启系统
sudo sysctl -p

# 验证
# 方法1：使用sysctl命令
sysctl vm.max_map_count

# 方法2：查看proc文件系统
cat /proc/sys/vm/max_map_count

# 方法3：使用更详细的sysctl查询
sysctl -n vm.max_map_count
```

#### 5、禁用透明大页
>透明大页（Transparent HugePages，简称THP）是Linux内核的一项内存管理功能，通俗地说，它试图自动将许多小块内存拼接成更大的内存块来使用，旨在提升性能，但并非在所有场景下都表现良好
什么场景下建议禁用：
- 运行数据库服务：许多主流数据库（如 Oracle、MySQL/MariaDB、MongoDB、Redis）的官方最佳实践明确建议禁用THP。这是因为数据库的内存访问模式往往随机且频繁，THP的动态合并可能带来不可预测的性能抖动，反而影响稳定性
- 对延迟极其敏感的应用：例如金融交易系统、实时计算等场景，需要稳定的响应时间。THP后台进程（khugepaged）进行内存整理时可能占用CPU周期，导致请求延迟增加

##### 5.1 查看和释义
- /sys/kernel/mm/transparent_hugepage/enabled：是否启用 透明大页分配
```shell
# 查看当前设置
cat /sys/kernel/mm/transparent_hugepage/enabled

# 可能的输出（方括号表示当前选中）：
always madvise [never]    # 当前选择 never（禁用）
[always] madvise never    # 当前选择 always（总是启用）
always [madvise] never    # 当前选择 madvise（按建议启用）
```

- /sys/kernel/mm/transparent_hugepage/defrag：如何整理内存碎片 来合并大页
```shell
# 查看当前设置
cat /sys/kernel/mm/transparent_hugepage/defrag

# 可能的输出：
always defer defer+madvise madvise [never]
# 当前选择 never（不整理）
```

##### 5.2 工作流程
当应用程序申请内存时：  
流程1：检查 enabled 设置
```text
应用程序申请内存
    ↓
检查 /sys/kernel/mm/transparent_hugepage/enabled
    ↓
if [never]: 使用普通 4KB 小页 ← 停止
if [always] 或 [madvise]: 继续下一步
```
流程2：检查 defrag 设置（如果 enabled 不是 never）
```text
需要分配大页但内存不连续
    ↓
检查 /sys/kernel/mm/transparent_hugepage/defrag
    ↓
if [never]: 分配失败，回退到小页
if [always]: 立即整理内存碎片（可能阻塞应用）
if [defer]: 标记需要整理，由 khugepaged 后台处理
```
##### 5.3 Redis 为什么要同时设置这两个为 never
场景模拟
```text
# Redis 运行时内存分配
1. Redis 启动时，enabled=always, defrag=always
2. Redis 申请 100MB 内存用于数据存储
3. 系统尝试分配 50个 2MB 大页
4. 但当前内存碎片化，没有连续 2MB 空间
5. defrag=always 触发立即内存整理
6. 整理期间 Redis 主线程被阻塞 ← 产生延迟峰值
7. 内存整理完成，分配大页
8. Redis 执行 BGSAVE，fork() 子进程
9. 子进程继承所有大页，写时复制导致内存翻倍 ← OOM 风险
```
##### 5.4 临时禁用透明大页
- 临时禁用
```shell
# 禁用 enabled：不使用大页，避免内存翻倍和延迟（重启失效）
echo never > /sys/kernel/mm/transparent_hugepage/enabled

# 禁用 defrag：即使意外启用大页，也不整理碎片（重启失效）
echo never > /sys/kernel/mm/transparent_hugepage/defrag
```
- 使用服务文件
```shell
vim /etc/systemd/system/disable-thp.service
```
```text
[Unit]
Description=Disable Transparent Huge Pages (THP)
After=sysinit.target local-fs.target

[Service]
Type=oneshot # 一次性服务,执行命令后退出.服务状态不会显示为active
ExecStart=/bin/bash -c 'echo never > /sys/kernel/mm/transparent_hugepage/enabled'
ExecStart=/bin/bash -c 'echo never > /sys/kernel/mm/transparent_hugepage/defrag'

[Install]
WantedBy=multi-user.target
```
```shell
systemctl daemon-reload
systemctl enable disable-thp.service
systemctl start disable-thp.service
```

#### 6、关闭防火墙
```shell
# 
systemctl status firewalld
systemctl stop firewalld
systemctl disable firewalld
systemctl is-enabled firewalld
```

#### 7、禁用selinux
SELinux是Linux内核中一个强大的强制访问控制安全模块。但是在docker、keepalived环境中建议关闭，否则容易出现问题
```shell
# 查看selinux状态
getenforce
# 
sestatus

# 永久关闭selinux
sed -i '/^[[:space:]]*SELINUX[[:space:]]*=/ s/enforcing/disabled/' /etc/selinux/config

# 临时关闭selinux
setenforce 0

# 检查文件是否修改成功
cat /etc/selinux/config

getenforce

sestatus
```
